#include <stdio.h>
int main(){

    int number,hours;
    float amount,result;

    scanf("%d", &number);
    scanf("%d", &hours);
    scanf("%f", &amount);

    result=hours*amount;
    printf("NUMBER = %d\n", number);
    printf("SALARY = U$ %.2f\n", result);


return 0;
}
